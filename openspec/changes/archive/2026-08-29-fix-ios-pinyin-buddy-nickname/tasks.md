## 1. 输入与校验实现

- [x] 1.1 移除添加/编辑饭搭子昵称输入框的原生 `maxlength` 即时截断，保留“最多 6 个字”提示。
- [x] 1.2 核对并保持 `addMember`、`updateMember` 共用的 Unicode 字符长度、空值和重复昵称校验，确保合法昵称正常保存、超长昵称可见报错。
- [x] 1.3 增加昵称超过 6 个 Unicode 字符时的输入框下方实时提示；提示不截断输入，恢复到合法长度后自动消失。

## 2. 验证与验收

- [x] 2.1 增加或更新可执行验证，覆盖 6 个字符边界、超长昵称、空昵称、重复昵称及编辑流程。
- [x] 2.2 运行 `pnpm type-check`。
- [x] 2.3 运行 `pnpm lint`。
- [x] 2.4 运行涉及页面交互的 `pnpm build:h5` 与 `pnpm smoke:ui`，并在本地 H5 验证实时超长提示。
- [x] 2.5 在 iOS 真机或模拟器使用中文拼音输入法验收：组合输入不会被提前截断，候选字确认后可保存合法昵称；同时确认微信小程序端保存规则不变。
