## 1. 数据模型与抽签规则

- [x] 1.1 定义饭搭子、饭局参与者快照、饭局阶段、抽签尝试、换签原因和历史记录的严格 TypeScript 类型，并加入持久化版本字段与异常数据安全默认值；为旧临时成员快照保留兼容读取。
- [x] 1.2 实现设备本地日期键工具，覆盖跨午夜与历史分组边界；日期和历史完成记录不参与新饭局概率计算。
- [x] 1.3 实现可注入随机源的纯等概率规则，覆盖所有饭搭子统一候选、随机边界和排除成员后的重新计算。
- [x] 1.4 新增独立 Pinia store，完成饭搭子的添加、编辑、自动角色分配、删除（可恢复）、彻底删除、恢复及饭局草稿状态管理；彻底删除不修改历史快照。
- [x] 1.5 在 store 中实现先持久化结果再进入揭晓阶段、重复触发锁定、中断恢复、跳过揭晓、完成打卡、不同阶段饭局删除和保留换签事件的状态流转。
- [x] 1.6 为抽签规则与 store 核心行为增加可执行测试或校验脚本，验证稳定成员 ID、历史快照、不同阶段饭局删除、结果唯一性、保存失败、人数不足和本地日期边界。

## 2. 插画静态资源与视觉技术样例

- [x] 2.1 建立饭后分工 SVG 资源目录与统一画布、色板、粗描边、透明背景和命名规范，并记录资源体积与小程序兼容约束。
- [x] 2.2 先手工制作煎蛋、番茄两个饭搭子角色，以及脏盘、干净盘和海绵 SVG 技术样例；确保不内嵌文字、远程图片、CSS 动画或复杂滤镜。
- [x] 2.3 在最小舞台组件中验证样例资源的 H5 与微信小程序尺寸、透明边缘、描边和缩放旋转观感，并根据结果修正统一规范。
- [x] 2.4 按通过验证的规范补齐饭团、小鱼、香菇、西兰花、辣椒、面包六个角色及可选水流拖尾资源。
- [x] 2.5 检查完整资源包的文件命名、引用路径、视觉一致性和总包体影响，移除未使用的概念或中间生成文件。

## 3. 饭搭子与饭局界面

- [x] 3.1 新增饭后分工独立页面与生成式页面配置，保持现有 Tabbar 不变，并为页面接入插画式背景、标题区和安全底部间距。
- [x] 3.2 实现首页状态化入口卡，覆盖无成员、待组局、已揭晓和已完成四种状态并导航到同一饭后分工页面。
- [x] 3.3 实现饭搭子管理界面，支持昵称校验、角色与主题色选择、编辑、关闭弹框、内容区点击不误关闭、删除确认、可恢复删除、已删除成员彻底删除二次确认及历史不丢失提示。
- [x] 3.4 实现本次饭局组建界面，支持从饭搭子列表预填上次选择、切换参与/休息/不在场、在组局阶段添加并立即写入成员库的饭搭子和少于两人时的禁用提示。
- [x] 3.5 实现首次使用空状态，引导用户添加至少两位饭搭子；添加流程不产生临时成员、试玩成员或事后保存转换。
- [x] 3.6 在决定前展示“本轮全员等概率”规则解释，所有饭搭子使用相同规则且不增加额外确认。

## 4. 洗牌与泡沫揭晓动效

- [x] 4.1 实现 2–8 人的成员贴纸舞台布局，以及 `editing`、`committing`、`shuffling`、`sealed`、`revealing`、`revealed`、`completed` 阶段与页面视图映射。
- [x] 4.2 使用 `transform` 与 `opacity` 实现成员弧线洗牌、聚拢、餐盘水波和泡沫封印，并确保动效只消费既定结果、不调用抽签逻辑。
- [x] 4.3 实现泡沫封印区域和自动清场揭晓状态，不要求用户完成擦拭操作，并处理动画中断与平台降级。
- [x] 4.4 实现获选角色弹出、规则说明、结果卡落地和少量一次性粒子，并提供始终可用的“跳过动效”操作。
- [x] 4.5 默认播放完整动效，不提供应用内精简动效开关；在可靠可用时读取系统减少动态效果偏好并降级为短淡入或缩放，保持同一结果。
- [x] 4.6 处理页面隐藏、进入后台、卸载和重新进入，依据持久化阶段继续揭晓或直接显示结果，验证不会二次抽取或卡在不可操作状态。

## 5. 结果、换签与历史

- [x] 5.1 实现结果页的获选成员与本轮全员等概率解释；删除饭搭子后仍保留历史快照和原规则展示。
- [x] 5.2 实现“洗完打卡”交互与脏盘到干净盘的简短完成动效，完成记录关联稳定饭搭子身份并保留历史快照。
- [x] 5.3 实现“今天有情况”操作面板，限定预定义原因、记录原结果、排除当前获选者后重新抽取，并处理剩余候选不足；面板支持明确关闭按钮、遮罩关闭且内容点击不误提交。
- [x] 5.4 实现本地历史列表与空状态，展示饭局日期、参与者快照、规则、每次换签、最终结果和完成状态，并支持删除任意阶段饭局。
- [x] 5.5 增加规则与本地数据边界说明，明确当前设备存储、不跨设备同步且不提供不可篡改保证。

## 6. 验证与交付

- [x] 6.1 更新 `scripts/smoke-ui.ts`，覆盖首页入口、饭后分工页、成员空状态、组局状态、跳过操作、结果卡、历史删除、弹框遮罩关闭与内容区阻止冒泡和插画样式关键契约。
- [x] 6.2 执行抽签规则与 store 的测试或校验脚本，记录所有边界场景的通过结果。
- [x] 6.3 执行 `pnpm type-check` 与 `pnpm lint`，修复本变更引入的类型和规范问题。
- [x] 6.4 执行 `pnpm smoke:ui`、`CHOKIDAR_USEPOLLING=1 pnpm build:h5` 与 `CHOKIDAR_USEPOLLING=1 pnpm build:mp-weixin`。
- [x] 6.5 在 390×844 H5 视口及微信小程序开发者工具或真机人工验收 2、4、8 人布局、Lottie 自动清场与降级、快速重复点击、后台恢复、跳过、系统减少动态效果降级、跨午夜和历史恢复。
- [x] 6.6 执行 `openspec validate add-dish-duty-draw --strict`，并在交付说明中记录所有未执行验证、平台差异和已知限制。

## 7. 同日多顿饭与再次发起

- [x] 7.1 调整 store 的当前饭局选择与创建逻辑，移除 `dateKey` 每日唯一假设；同日饭局使用独立 ID 共存，恢复时选择最新未完成饭局或当天最新完成饭局。
- [x] 7.2 实现完成结果上的“再开一局”，保留旧饭局并以上一局参与状态预填新草稿；防止快速重复点击创建多个草稿或覆盖历史。
- [x] 7.3 更新首页入口、饭后分工页与历史列表，使当天最新一局驱动状态，并让同日多顿饭按创建时间分别展示且可区分。
- [x] 7.4 移除近 7 日完成次数对候选与概率的影响；日期和完成记录仅用于历史分组与展示。
- [x] 7.5 扩展抽取与 store 校验脚本，覆盖同日连续完成两局、旧记录不覆盖、最新未完成恢复、再次发起锁定、历史记录不影响新抽取和跨午夜行为。
- [x] 7.6 更新 UI smoke 与 390×844 H5 人工流程，验证完成后再开一局、同日历史多条、首页最新状态和新一局独立结果；重新执行类型、lint、双端构建与 `openspec validate add-dish-duty-draw --strict`。

## 8. 餐盘漩涡抽签与擦拭体验升级

- [x] 8.1 重构抽签舞台的参与者布局与轨迹配置，为 2、3、4、5–8 人提供稳定模板，并实现加速旋转、至少一次可辨识换位、向餐盘聚拢与急停；不得退化为固定坐标原地摆动。
- [x] 8.2 将泡沫视觉重构为 3–5 组轻量云团与少量小泡泡；调整层级和初始位置，确保泡沫不遮挡成员昵称、餐盘中心或操作提示。
- [x] 8.3 增加跨端 Lottie 播放适配层，H5 与微信小程序自动播放清场；播放器或 H5 容器引用不可用时仍降级为 CSS/静态清场并自动完成阶段流转，同时处理暂停、取消和页面离开。
- [x] 8.4 制作并接入漩涡、泡沫卷走、水花和中奖爆发 Lottie 资源；动画不内嵌成员昵称、角色或抽签逻辑，并限制包体和特性集合。
- [x] 8.5 在 `sealed` 阶段自动播放清场，短暂停顿后完成揭晓；获选成员成为中心焦点，未获选成员退场或弱化，揭晓全过程只消费既定结果且只触发一次。
- [x] 8.6 调整“跳过动效”：跳过始终可用并立即展示既定结果；结算态不提供重播入口；精简模式仅保留约 350ms 的聚拢、短促清场和淡入或缩放揭晓。
- [x] 8.7 扩展动效校验与 UI smoke，覆盖 Lottie 资源存在性、播放器降级、后台恢复、跳过、精简模式结果一致性，以及微信 scoped WXSS 不生成通配后代选择器。
- [x] 8.8 执行 `pnpm verify:dish-duty`、`pnpm type-check`、`pnpm lint`、`pnpm smoke:ui`、H5 与微信小程序构建，并在 390×844 H5 及微信开发者工具或真机验收 2、3、4、8 人洗牌节奏、泡沫层级、Lottie 自动清场与降级、清场爆发、跳过和无横向溢出；最后执行严格 OpenSpec 校验。
- [x] 8.9 优化 `revealed` / `completed` 结算态视觉层级：隐藏洗牌装饰、弱化餐盘、保留获选角色与爆发装饰；移除舞台内独立结果章，让舞台下方结果卡作为唯一结算信息源；结算后将舞台收紧至约 `500rpx` 并重新居中内容，同时保持动画阶段原高度，避免中心重叠、信息重复和大面积空白。
