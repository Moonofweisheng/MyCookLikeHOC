## 1. 朋友圈分享实现

- [x] 1.1 在 `src/pages/dish-duty/index.vue` 注册 `onShareTimeline`，以 `showResult` 与 `currentWinner` 共同生成通用或带获选人昵称的朋友圈标题。
- [x] 1.2 保持朋友圈分享目标为当前饭后分工页且不附带成员、饭局、结果或历史数据 query，并确认不改变现有好友分享与 H5 页面布局。

## 2. 自动化验证

- [x] 2.1 更新 `scripts/smoke-ui.ts`，检查朋友圈分享钩子、可见结果标题分支和无业务 query 的页面契约。
- [x] 2.2 执行 `pnpm type-check`、`pnpm lint` 和 `pnpm smoke:ui`，修复本变更引入的全部失败。
- [x] 2.3 执行 `pnpm build:h5` 与 `pnpm build:mp-weixin`，确认 H5 保持可构建且微信小程序产物包含朋友圈分享能力。

## 3. 微信端人工验收

- [x] 3.1 在微信开发者工具或真机分别验证未开始抽签、揭晓动效中、已揭晓和已完成四种状态：朋友圈入口存在，前两种使用通用标题，后两种使用当前获选人标题。
- [x] 3.2 在微信开发者工具中打开系统“小程序分享页”独立环境，确认进入饭后分工页后以空本地状态初始化，不恢复发送端的成员、结果或历史；真实朋友圈发布与另一台真机打开未执行，并已记录为残余风险。
