# GlobalDialog 全局弹窗组件

GlobalDialog 是基于 `@wot-ui/ui` 的 `wd-dialog` 组件封装的全局弹窗组件，通过 Pinia 状态管理实现全局调用，用于显示提醒、确认、输入等交互场景。

- 基于 `wd-dialog` 组件封装
- 使用 `useGlobalDialog` 调用
- 支持 `show`、`alert`、`confirm`、`prompt` 和 `close`
